import { palette } from '../Theme';

export default {
  header: {
    top: 0,
    right: 0,
    zIndex: 1,
    height: '48px',
    background: palette.primary,
    margin: 0,
    padding: 0,
    position: 'absolute',
    width: '100%',
  },
};
