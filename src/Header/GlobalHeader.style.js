import { palette } from '../Theme';

export default {
  header: {
    top: 0,
    right: 0,
    zIndex: 3,
    height: '48px',
    background: palette.blues.Independence,
    margin: 0,
    padding: 0,
    position: 'absolute',
    left: 0,
  },
};
