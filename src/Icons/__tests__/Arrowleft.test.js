import React from 'react';
import Arrowleft from '../Arrowleft';

describe('Icon : <Arrowleft />', () => {
  it('match snapshot', () => {
    expect(shallow(<Arrowleft />)).toMatchSnapshot();
  });
});


