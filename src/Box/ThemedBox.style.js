import { palette } from '../Theme';
export default {
  dark: {
    backgroundColor: palette.primary.main,
    color: '#fff',
  },
  light: {
    backgroundColor: '#fff',
    color: palette.semantic.default
  },
};
