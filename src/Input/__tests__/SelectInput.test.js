// import React from 'react';
// import SelectInput from '../SelectInput'; // eslint-disable-line

describe('<SelectInput />', () => {
  it('it cannot have test apparently for some reason, try it and let me know, just uncomment line 2 and unr the test -df', () => {
    expect(true).toEqual(true);
  });
  // it('exists', () => {
  //   expect(SelectInput).toBeDefined();
  // });

  // it('matches snapshot', () => {
  //   const sut = shallow(
  //     <SelectInput />
  //   );
  //   expect(sut).toMatchSnapshot();
  // });
});
