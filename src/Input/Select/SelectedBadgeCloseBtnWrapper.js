import styled from 'styled-components';


const SelectedBadgeCloseBtnWrapper = styled.div`
  button:focus,
  button:hover {
    font-weight: bold;
    transform: scale(1.02);
  }
`;

export default SelectedBadgeCloseBtnWrapper;
