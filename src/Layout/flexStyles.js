import { css } from 'styled-components';


export const flexStyles = {
  background: 'transparent',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
};

export const flexStylesCSSString = css`
  background: transparent;
  display: flex;
  justify-content: center;
  align-items: center;
`.join('');

export default null;
